defaults:
  - normalbc_pretrained_actor
  - _self_

runner:
  logger:
    experiment_name: "robotwin_train_giga_world_policy_freeze_visual"

algorithm:
  training_stage: full_rl
  freeze_critic_updates: false
  freeze_actor_updates: false
  freeze_visual_layers: true
